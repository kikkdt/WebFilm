/***** Main Content *****/
.main-content {
    position: relative;
    top: 60px;
    left: 250px;
    @include calc(width, "100% - 250px");
    height: 100%;
    padding: 1.625rem 0 0 0;
    overflow-x: hidden;
}

.content {
    min-height: calc(100vh - 250px);

    .swiper-container,
    .swiper-slide {
        width: 100%;
        height: 300px;
        border-radius: 6px;
    }

    .swiper-container {
        .swiper-pagination-bullet {
            width: 1rem;
            height: 1rem;
            border: 2px solid #e0e6ed;
            background: $color-dark-blue;
        }

        .swiper-button-prev,
        .swiper-button-next {
            color: #e0e6ed;
        }

        .swiper-button-prev:after,
        .swiper-button-next:after {
            font-size: 2rem;
            font-weight: 900;
        }
    }

    .swiper-slide {
        img {
            width: 100%;
            height: 100%;
        }
    }

    .tag-bar {
        width: 100%;
        display: grid;
        grid-template-columns: repeat(auto-fill, minmax(100px, 1fr));
        grid-gap: 1rem;
        margin: 1rem 0;
        padding: 1rem;
        text-align: center;
        background: #0e1726;
        border-radius: 6px;

        .tag-bar__item {
            list-style: none;
            background: $color-dark-blue;
            color: #e0e6ed;
            padding: .25rem;
            border-radius: 6px;

            a {
                color: #506690;
                font-weight: 600;
            }
        }

        .tag-bar__item:hover,
        .tag-bar__link:hover {
            .tag-bar__item {
                background: rgba(67, 97, 238, 0.25);
                transition: all .6s ease;
            }

            a {
                color: #e0e6ed;
            }
        }
    }
}

.modal-content {
    background: #0e1726;
    border-radius: 6px;

    .modal-header {
        border-bottom: none;
        flex-direction: column;

        .btn-close {
            color: #fff;
            background: none;
        }
    }

    .modal-title {
        color: $color-red;
        font-weight: 700;
        font-size: 2rem;
    }

    .modal-footer {
        justify-content: center;
        text-align: center;

        span {
            font-weight: 700;
            font-size: 1.125rem;
        }
    }
}

.list-movie__container,
.table-container {
    width: 100%;
    height: 100% !important;
    margin: 0 0 1rem;
    padding: 1rem;
    background: #0e1726;
    border-radius: 6px;

    .list-movie__title {
        margin-bottom: 1rem;
        color: #e0e6ed;
        font-weight: 700;
    }

    .movie-card {
        width: 100%;
        height: 100%;

        .movie-card__img {
            position: relative;
            width: 100%;
            height: 300px;

            img {
                width: 100%;
                height: 100%;
                border-radius: 6px;
            }

            .fa-play-circle {
                display: none;
                position: absolute;
                font-size: 4rem;
                top: 50%;
                left: 50%;
                transform: translate(-50%, -50%);
                color: $color-red;
            }
        }

        .movie-card__img:hover {
            img {
                opacity: .65;
            }

            .fa-play-circle {
                display: block;
                transition: all .6s ease;
            }
        }

        .movie-card__detail {
            display: flex;
            flex-direction: column;
            justify-content: space-around;
            align-items: flex-start;
            margin: .5rem 0;

            .moive-card__detail__title-vi {
                color: $color-red;
                font-size: 1.125rem;
                font-weight: 700;
                display: -webkit-box;
                -webkit-line-clamp: 1;
                -webkit-box-orient: vertical;
                overflow: hidden;
            }

            .moive-card__detail__title-en {
                color: $color-grey;
                display: -webkit-box;
                -webkit-line-clamp: 1;
                -webkit-box-orient: vertical;
                overflow: hidden;
            }

            .fas {
                margin-right: .5rem;
            }

            .viewed:hover {
                .viewed,
                .fa-eye {
                    color: $color-red;
                }
            }
        }
    }

    .list-movie__grid {
        width: 100%;
        height: 100%;
        display: grid;
        grid-template-columns: repeat(auto-fit, 18%);
        grid-gap: 1rem;
    }
}

.info-movie {
    width: 100%;
    height: 100%;
    margin: 1rem 0;
    padding: 1rem;
    background: #0e1726;
    border-radius: 6px;

    .info-movie__poster {
        img {
            width: 100%;
            height: auto;
            border-radius: 6px;
        }
    }

    .info-movie__title-vi {
        color: $color-red;
        font-weight: 700;
    }

    .btn-group {
        display: grid;
        grid-template-columns: repeat(3, 1fr);
        justify-content: center;
        margin: .5rem 0;

        .btn:hover,
        .viewed:hover {
            color: $color-grey;
            transition: all .3s ease;

            .fas {
                color: $color-grey;
            }
        }

        .btn:focus {
            box-shadow: none;
        }

        .btn-play,
        .viewed {
            min-width: 125px;
            min-height: 60px;
        }

        .btn-play {
            border: 3px solid $color-red !important;
            border-radius: 6px !important;
        }

        .btn-play:hover {
            border: 3px solid $color-grey;
        }

        .btn,
        .viewed,
        .fas {
            color: $color-red;
            font-size: 1.5rem;
            font-weight: 700;
        }

        .fas {
            margin-right: .5rem;
        }
    }

    .description {
        text-align: justify;
    }

    .more-info {
        li {
            list-style: none;
        }

        span {
            font-weight: 700;
        }
    }
}

.page-nav {
    margin-top: 2rem;

    span {
        display: flex;
        justify-content: center;
        align-items: center;
    }

    .page-button,
    .current-button {
        font-weight: 600;
        background: #0e1726;
        border: 1px solid #383f46;
        border-radius: 50%;
        color: $color-red;
        padding: .5rem 1rem;
        margin-right: .3rem;
    }

    .page-button:last-child {
        margin-right: 0;
    }

    .current-button {
        background: $color-red;
        color: #fff;
    }
}

#player {
    display: flex;
    justify-content: center;
    align-items: center;
    margin: 5rem 0;
    min-height: 420px;
}